.row
  .col-md-12
    .page-header
      %h1
        Edit
        %em #{proposal.title}
        for #{event}

= simple_form_for proposal, url: proposal_path(event.slug, proposal) do |f|
  = render partial: 'form', locals: {f: f}
