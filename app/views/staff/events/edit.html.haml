.row
  .col-md-12
    %header.page-header
      %h1
        Edit #{event}

.row
  .col-md-12
    %span.required_notification * Required
    = simple_form_for event, url: event_staff_update_path(event), html: {role: 'form'} do |f|
      = render partial: 'staff/events/form', locals: {f: f}
