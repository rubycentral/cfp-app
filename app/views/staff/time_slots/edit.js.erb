var editDialog = $('#time-slot-edit-dialog');

editDialog[0].innerHTML =
    '<%=j render partial: 'edit_dialog',
      locals: { time_slot: time_slot_decorator, event: event } %>';

setUpTimeSlotDialog(editDialog);
