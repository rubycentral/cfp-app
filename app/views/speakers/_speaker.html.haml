- withdraw = true if withdraw.nil?

.speaker.clearfix
  %strong= speaker.name

  - if  current_user && speaker_for_proposal?(current_user) && withdraw && speaker.proposal.speakers.count > 1
    %p= link_to "Withdraw",
        speaker_path(speaker.id),
        method: :delete,
        class: "btn btn-danger btn-sm pull-right remove-speaker",
        data: { confirm: "This will remove #{speaker.name} from this proposal. Are you sure you want to do this?" }

  = simple_format speaker.bio
%hr
