.draggable-session-card{data: ps.ps_data.merge({ draggable_sessions_target: "session" })}
  .track
    %span=truncate(ps.track_name, length: 18)
  .session-card-body
    .title=truncate(ps.title, length: 31, separator: ' ')
