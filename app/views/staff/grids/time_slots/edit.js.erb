var editDialog = $('#grid-time-slot-edit-dialog');

editDialog.html('<%=j render partial: 'edit_dialog',
      locals: { time_slot: time_slot_decorated, event: event } %>');

setUpTimeSlotDialog(editDialog);
